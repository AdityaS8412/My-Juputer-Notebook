# Aditya Shinde
#I am intested in data science because i love to gain insights for data
#The folloein code tests the gauss formula


    def gauss(n):
    return (n*(n+1))/2
gauss(100) == sum(range(101))
